query NodesDetails(
  $mode: Mode!,
  $bridge: Bridge!
) {
    nodes(mode: $mode, bridge: $bridge) {
      id
      mode
      bridge
      status
      ip
      ipRegionCode
      version
      moniker
      restUri
      p2pHost
      rewardsAddress
      lastSeenAt
      bondAmount
      bondExpiresAt
      bondFraction
    }
  }
